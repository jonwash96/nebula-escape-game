export class User {
    static player;
    constructor(object) {
        User.player++;
    }
}

// Registration -> Modal -> submit
// on submit => Registration ->> Create new User()
// Create user object
// Create new Board() & asign

const user = {
    name,
    player,
    ships,
    side,
    hits,
    health,
    board,
    placeShips() {},
    attack() {},
}

function createNewUser() {
    const root = document.querySelector()
}
